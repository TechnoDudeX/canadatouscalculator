{
  "name": "canadatouscalculator",
  "version": "4.0.0",
  "private": true,
  "description": "North×South — Canada-to-US compensation honesty audit. Static site, no build step.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build:css": "tailwindcss -i src.css -o style.css --minify",
    "watch:css": "tailwindcss -i src.css -o style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19",
    "vitest": "^2.1.0"
  }
}
