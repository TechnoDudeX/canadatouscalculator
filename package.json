{
  "name": "canadatouscalculator",
  "version": "3.3.0",
  "private": true,
  "description": "North×South — Canada-to-US compensation honesty audit. Static site, no build step.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
